import { useState } from "react";

function App() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <h1 className="head-text">React App</h1>
    </div>
  );
}

export default App;
